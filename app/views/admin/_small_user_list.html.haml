%table.table.table-bordered.table-striped.table-hover
  %thead
    %tr
      %th Username
      %th Name
      %th Email

  %tbody
    - @users.each do |user|
      %tr
        %td= user.username
        %td= user.real_display_name
        %td= user.email
