= render partial: "infographs/data_filter", locals: {selection_options: selection_options}

#graph-panel
  - if tables.count > 0
    - tables.each do |table|
      = render partial: 'infographs/data_panel', locals: {table_data: table}

