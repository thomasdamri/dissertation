-# Partial form for student tasks
.col 
  = simple_form_for @task, url: student_task_create_path(@student_team.id) do |f|
    .row.pt-1.mb-3
      .col.form-group.mb-0
        = f.text_area  :task_objective, required: true, as: :string, placeholder: "Whats the objective?", rows: 3, class: 'form-control'

    .row.form-group
      .col-auto.mb-0
        = f.label :task_target_date, "Target date?"
      .col.mb-0
        = f.input :task_target_date, start_year: Date.today.year, lable: false, class: 'form-control bp-1 mb-0', label: false

    .row.justify-content-between.form-group
      .col-auto
        = f.label :task_difficulty, "How difficult?"
      .col-auto
        = f.input_field :task_difficulty, as: :select, collection: ["Easy", "Medium", "Hard"], input_html: {multiple: false}, prompt: "Difficulty", class: 'form-control'

      .col 
        = f.button :submit, 'Post', class: 'btn btn-primary float-right'
