%h1= @assessment.name
%h3= "Viewing this assessment as team #{@team.number}"

= form_tag do |f|
  - @assessment.questions.each do |crit|
    = render 'question/answer_fields', crit: crit

= link_to "Back to assessment", @assessment, class: 'btn btn-secondary'